{
  "name": "pi-llama-server",
  "version": "1.1.0",
  "description": "Pi extension for llama-server router — dynamic model properties via /props, live model listing, load/unload, per-project config",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "llama.cpp",
    "llama-server"
  ],
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./extensions/llama-server.ts"
    ]
  }
}
